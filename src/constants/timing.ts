/**
 * Animation and timing constants.
 */

export const TIMING = {
  SHAKE_DURATION_MS: 500,
  SANITY_TRANSITION_MS: 1000,
  FILTER_TRANSITION_MS: 200,
} as const;

export const THRESHOLDS = {
  // Brightness threshold to show answer input in MagicLens
  BRIGHTNESS_REVEAL: 150,
} as const;
